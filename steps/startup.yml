- clean: ['~']

- shell:
  - sudo apt-get update
  - sudo apt-get upgrade
  - sudo apt install gnome-tweaks
  - echo "Make below tweaks using gnome tweaks"
  - echo "Add slack to startip application"
  - echo 'windows → window focus → "focus on hover" & check "raise windows when focused"'
  - echo 'workspaces → display handling → workspaces span display'
  - sudo apt install curl
  - echo "remove snaps [*_*]"


